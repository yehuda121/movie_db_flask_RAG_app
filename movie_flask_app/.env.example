# Copy to .env and fill in your key (never commit .env to Git)
GROQ_API_KEY=your_groq_api_key_here
FLASK_DEBUG=1
SECRET_KEY=change-this-for-learning-only
# Set to 1 to log retrieved chunks, scores, and prompts to the console
RAG_DEBUG=0
RAG_MIN_CONTEXT_SCORE=0.40
